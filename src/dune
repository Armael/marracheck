(executables
  (names compute_cover worker marracheck)
  (modules compute_cover worker marracheck)
  (libraries opamcheck2_lib))

(executable
  (name opam_bin_cache)
  (modules opam_bin_cache)
  (libraries bos fpath rresult))